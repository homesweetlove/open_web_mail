# Nocturne Mail Verification Notes

## 2026-08-12

- Desktop preview at 1280x900 rendered the full asymmetric workspace: persistent sidebar, mail list, reading pane, and context rail.
- Mobile preview at 390x844 rendered the compact header, single-column mail list, search controls, and responsive reading flow.
- The `새 메일` action opened the compose dialog with recipient, subject, body, attachment, discard, close, and send controls.
- TypeScript and production build both passed with `pnpm check && pnpm build`.
- Generated image URLs are referenced directly from the project lifecycle storage: brand mark, lacquer seam texture, studio digest, and patina material.
- No real mail server or external send action is wired; compose and message actions are intentionally local UI demonstrations.
